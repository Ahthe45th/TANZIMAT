### Current Actions
~~- [ ] Think about what events should have file triggers for me.~~
- ~Set up so reminders can be run on certain days of the month and the quarter~
- ~Set up so reminders can send emails on the back of that~
- ~Set up so reminders can be run every x days~
- ~Create backbone of a system that gives me things to remember if I'm going to do thing x (mostly just think about how it would work)~
~~- Every day there needs to be an agenda and the target times for when everything is gonna be done so reminders coming up~~
~~- Set up an easy way to view current schedule~~
~~- Add to schedule all menial tasks so they are covered~~

### Notes
- In terms of stuff that should have file triggers:
- When a download is saved to what we call a /tmp directory it should notify of the next time this thing is likely to be swept away
- Whenever the hosts file is changed there should be a trigger that asks what to put back and after how long automatically
- When the alarm goes off a watcher should check if the phrase was written correctly and if a shortcut was taken just start the alarm again
- Got a script from chatgpt that keeps reminders for any given ritual now to just based on a schedule set up so I'm reminded of things at certain times

### Completed Actions
~~- Reminders.json needs to come back up~~
- ~Set up so reminders can also be on certain days of the week~
- ~Reminder can run command on the back of it~
- ~Set up reminders and system for reminders~
